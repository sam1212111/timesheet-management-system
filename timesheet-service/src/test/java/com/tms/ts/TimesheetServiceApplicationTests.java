package com.tms.ts;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

@Disabled("Context smoke test requires external infrastructure and is excluded from unit verification")
class TimesheetServiceApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally left as a disabled smoke-test placeholder for local unit verification.
        assertTrue(true);
    }
}
